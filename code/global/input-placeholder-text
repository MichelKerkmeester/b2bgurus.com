// Input field
// Adjustable placeholder text
$(document).ready(function () {
  $("input, textarea").each(function () {
    $(this).attr("placeholder", $(this).attr("data-placeholder")); // Set placeholder attribute from data-placeholder
  });
});
